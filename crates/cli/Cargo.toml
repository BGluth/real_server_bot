[package]
name = "reals_server_bot_cli"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
clap = { version = "4.5.40", features = ["derive"] }
reals_server_bot_common = { path = "../common" }
